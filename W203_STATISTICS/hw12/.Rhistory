install.packages("sandwich")
library(car)
library(lmtest)
library(sandwich)
model1 = lm(mpg ~ disp + hp + wt + drat, data = data)
ls()
library(car)
car
car
?car
data = mtcars
model1 = lm(mpg ~ disp + hp + wt + drat, data = data)
plot(model1)
plot(data$disp, data$hp)
plot(data$hp, data$wt)
plot(data$hp, data$drat)
